# todo-list
Practice C# project using .NET and React

To run frontend (http://localhost:3000):
```bash
cd task-manager
npm install
npm start
```

To run backend API (http://localhost:5127):
```bash
cd TaskManagementApi
dotnet restore
dotnet run
```